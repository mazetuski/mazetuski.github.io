

{{ form_start (form,
    {'action':path('blogger_blog_comment_create',{'blog_id' : comment.blog.id}),
        'method' : 'POST', 'attr' : {'class':'blogger'}}
    )
}}
{{ form_widget(form) }}
{{ form_end(form) }}